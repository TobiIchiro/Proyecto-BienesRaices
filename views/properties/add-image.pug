extends ../layout/admin

block styles
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/dropzone.css")
    meta(name= "csrf-token" content=csrfToken)

block content
    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center Bienes
            span.font-normal Raices
        h2.text-center.text-2xl.font-extrabold= pagina
        div(class= "bg-white shadow py-8 px-4 rounded mx-auto mx-w-4xl my-10 md:px-10")
            form(id= "image" class= "dropzone border-dashed border-2 w-full min:h-96 h-auto" method= "POST" action=`/my-properties/add-image/${property.id}`)
                input(type="hidden" name="_csrf" value=csrfToken)
                
            input(class= "mt-5 w-full py-3 bg-gray-700 hover:bg-gray-800 rounded text-white font-bold uppercase cursor-pointer" type= "submit" value="Publicar propiedad")

block scripts
    script(src='/js/addImage.js')