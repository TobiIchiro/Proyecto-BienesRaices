extends ./layout/app

block styles 

block content 
    section(class= "py-5")
        h2(class= "text-center text-4xl font-extrabold pt-10")= pagina
        if properties.length
            div(class= "grid grid-cols-1 lg:grid-cols-3 gap-5 mt-5")
                each propiedad in properties
                    include ./templates/card
        else
            p(class= "text-center text-gray-800 text-md py-10") No hay resultados